// =============================================================
// src/config/supabase.js — DEPRECATED
// =============================================================
// Plantea now uses a self-contained SQLite database
// (src/config/db.js) so the whole platform runs 100% free with
// no external accounts. This file is kept as a compatibility
// stub — it no longer connects to Supabase.
// =============================================================

module.exports = require('./db');
