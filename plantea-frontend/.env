# Backend API Configuration
# In production the app and API are served from the same origin, so a
# relative base works (see plantea-backend/server.js static serving).
# For physical devices run: node plantea-frontend/scripts/setup-local.js
EXPO_PUBLIC_API_BASE_URL=/api
EXPO_PUBLIC_API_TIMEOUT=10000
